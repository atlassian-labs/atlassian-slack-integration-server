package com.atlassian.jira.plugins.slack.model.event;

/**
 * This is the base of all the events that we can process and send notifications for. This should really be called
 * Event, but there are already dozens of Events in classpath.
 */
public interface PluginEvent {
}
